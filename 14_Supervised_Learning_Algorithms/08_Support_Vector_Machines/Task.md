# Task: Cancer Classification (SVM)

## Objective
Use SVM to classify benign vs malignant tumors.

## Data
`sklearn.datasets.load_breast_cancer()`

## Tasks
1.  **Train**: Fit an `SVC` with `kernel='linear'`.
2.  **Evaluate**: Check accuracy.
3.  **Kernel Trick**: Try `kernel='rbf'`. Does accuracy improve?
